@using Demo.Models.ViewModels
@model FormFieldModel

<div class="row my-1">
    <div class="col-3">@Model.Label</div>
    @if(Model.IsReadOnly || (!User.HasAdminRights() && Model.RequiresRoleValidation))
    {
        <div class="col-9">
            @Html.TextBox(Model.FieldName, Model.Value,
                new
                { 
                    @class = "form-control", 
                    @readonly = "readonly" 
                })
        </div>
    }
    else
    {
        <div class="col-9">
            @Html.TextBox(Model.FieldName, Model.Value, new { @class = "form-control" })
        </div>
    }
</div>
